const prompt = require('prompt-sync')();
let a=parseFloat(prompt('enter the number'))
const oddorEven=(a)=>{
    if(a%2==0){
        console.log(`a:${a} is even`);
    }else{
        console.log(`b:${b} is even`);
    }
}
const evenOrodd=oddorEven(a)
console.log(evenOrodd);
